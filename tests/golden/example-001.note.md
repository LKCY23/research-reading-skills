# Layer 1 validation note

`example-001` is currently the repository's primary Layer 1 quasi-golden example.

If a future change alters:
- schema field names
- prompt output contracts
- paper-card rendering structure
- claim/evidence expectations

then this example should be reviewed first.
