# Example 001: Literature-review scaffold placeholder

This directory is a structural placeholder for the first Layer 2 literature-review example.

It is not a completed real literature review. Its purpose is to show the required file set and naming conventions for a scaffold-level Layer 2 example.

## Required files

- `README.md` — explains the scaffold and required artifacts
- `topic-scope.json` — minimal `topic_scope` example artifact
- `paper-set.json` — minimal `paper_set` example artifact
- `comparison-matrix.json` — minimal `comparison_matrix` example artifact
- `literature-review.json` — minimal aggregate `literature_review` artifact
- `literature-review.md` — placeholder human-readable rendering consistent with the template
